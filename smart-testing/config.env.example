# Discord webhook configuration
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/xxxxxxxxxxxxxxReplaceMeWithWebHookStringxxxxxxxxxxxxxxxxxxxx

# Drive Configuration
# For multiple drives, use comma-separated list (no spaces)
# Example: DRIVES=/dev/sda,/dev/sdb,/dev/nvme0n1
DRIVES=/dev/nvme0

# Test parameters
SMART_TIMEOUT=600

# Results directory path - each drive will have its own file in this directory
SMART_RESULTS_DIR=/tmp/smart_results
